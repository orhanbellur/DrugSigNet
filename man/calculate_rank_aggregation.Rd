% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/harmonize_signature_results.R
\name{calculate_rank_aggregation}
\alias{calculate_rank_aggregation}
\title{Aggregate signature rank tables with one rank aggregation method}
\usage{
calculate_rank_aggregation(
  method,
  ...,
  rank_inputs = NULL,
  prior = 0.093,
  num_bin = 200,
  ties_method = "max"
)
}
\arguments{
\item{method}{Rank aggregation method. One of \code{"CRank"},
\code{"Dowdall"}, or \code{"RRA"}.}

\item{...}{Processed signature rank tables containing \code{perturbation}
and \code{rank_score} columns.}

\item{rank_inputs}{Optional list of processed signature rank tables. When
supplied, \code{...} is ignored.}

\item{prior}{Prior passed to \code{CRank()}.}

\item{num_bin}{Number of bins passed to \code{CRank()}.}

\item{ties_method}{Ties method passed to rank aggregation methods.}
}
\value{
A \code{RankAggregation} object.
}
\description{
This implementation is intentionally compatible with the original
Harmonize_Signature_Network() signature harmonization behaviour:
  - input rows are preserved as-is (no duplicate collapsing),
  - missing rank_score values remain NA (no NA -> 0 replacement),
  - the identifier remains `perturbation` during the first aggregation stage,
  - rank input columns are generated dynamically as rank_score_1 ... rank_score_n.
}
