#!/bin/sh

# DrugSigNet has no compile-time configuration. In particular, optional R
# packages must never be installed or altered here: configure runs inside R's
# staged-install transaction and may also run while vignette dependencies are
# loaded. Dependency installation belongs to the package manager.
exit 0
